machine:
  services:
    - docker

dependencies:
  pre:
    - go get github.com/tools/godep
  override:
    - mkdir -p $GOPATH/src $GOPATH/bin
    - export GOBIN=$GOPATH/bin
    - docker login --username=$DOCKERHUB_USERNAME --email=$DOCKERHUB_EMAIL --password=$DOCKERHUB_PASSWORD

test:
  override:
     - make
deployment:
  prod:
    branch: master
    commands:
      - docker rename autobuilder beamonlabs/autobuilder:latest
      - docker rename autocv beamonlabs/autocv:latest
      - docker push beamonlabs/autocv:latest
      - docker push beamonlabs/autobuilder:latest
  dev: 
    branch: dev
    commands:
      - docker rename autobuilder beamonlabs/autobuilder:dev
      - docker rename autocv beamonlabs/autocv:dev
      - docker push beamonlabs/autocv:dev
      - docker push beamonlabs/autobuilder:dev
